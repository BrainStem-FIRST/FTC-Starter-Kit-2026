pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        // Dairy Foundation: hosts the Sloth hot-reload Gradle plugin (dev.frozenmilk:Load).
        maven { url = 'https://repo.dairy.foundation/releases' }
    }
}

include ':FtcRobotController'
include ':TeamCode'
